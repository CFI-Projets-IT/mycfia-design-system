<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}myCFiia{% endblock %}</title>
    <link rel="icon" href="{{ asset('images/logo_picto.svg') }}" type="image/svg+xml">

    {% block stylesheets %}
        {{ importmap('app') }}
    {% endblock %}
</head>
<body class="theme-{{ app.user ? app.user.theme : 'light' }}">
    <div class="home-layout">
        {# Bouton hamburger pour mobile (offcanvas) #}
        <button class="btn btn-primary d-lg-none position-fixed top-0 start-0 m-3" style="z-index: 1050;" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarOffcanvas" aria-controls="sidebarOffcanvas">
            <i class="bi bi-list fs-4"></i>
        </button>

        {# Sidebar - Fixed sur desktop, Offcanvas sur mobile #}
        <aside class="home-sidebar d-none d-lg-flex flex-column position-fixed top-0 start-0 bottom-0" style="width: 280px; background-color: var(--theme-sidebar-bg); color: var(--theme-sidebar-text); z-index: 100;">
            {# Header avec icônes - toujours visible #}
            <div class="sidebar-header d-flex align-items-center justify-content-between mb-4 pb-3 px-3 pt-4">
                <button class="btn btn-link text-white-50 p-0" type="button" onclick="toggleSidebar()" aria-label="Menu" style="font-size: 2rem;">
                    <i class="bi bi-list"></i>
                </button>
                <button class="btn btn-link text-white-50 p-0" type="button" onclick="toggleSidebar()" aria-label="Fermer la sidebar" style="font-size: 2rem;">
                    <i class="bi bi-arrow-left-circle"></i>
                </button>
            </div>

            {# Navigation - Flex 1 pour occuper l'espace disponible #}
            <nav class="flex-grow-1 overflow-auto px-3">
                {# Section Favoris (collapsible avec fond) #}
                <div class="nav-section">
                    <div class="nav-section-title-pill" data-bs-toggle="collapse" data-bs-target="#favorisSection" aria-expanded="true">
                        <div class="d-flex align-items-center gap-2">
                            <i class="bi bi-star"></i>
                            <span>{{ 'nav.section.favorites'|trans({}, 'navigation') }}</span>
                        </div>
                        <i class="bi bi-chevron-down"></i>
                    </div>
                    <div class="collapse show" id="favorisSection">
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                    <span>{{ 'nav.favorites.invoices'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                    <span>{{ 'nav.favorites.orders'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                    <span>{{ 'nav.favorites.stocks'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                    <span>{{ 'nav.favorites.help'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>

                {# Section Historique (collapsible avec fond) #}
                <div class="nav-section">
                    <div class="nav-section-title-pill" data-bs-toggle="collapse" data-bs-target="#historiqueSection" aria-expanded="false">
                        <div class="d-flex align-items-center gap-2">
                            <i class="bi bi-clock-history"></i>
                            <span>{{ 'nav.section.history'|trans({}, 'navigation') }}</span>
                        </div>
                        <i class="bi bi-chevron-down"></i>
                    </div>
                    <div class="collapse" id="historiqueSection">
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                    <span>{{ 'nav.history.last_conversation'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                    <span>{{ 'nav.history.invoices_jan'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                    <span>{{ 'nav.history.stock_analysis'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                    <span>{{ 'nav.history.orders_report'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                            <li class="mb-2">
                                <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                    <span>{{ 'nav.history.data_export'|trans({}, 'navigation') }}</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>

            {# Footer avec Paramètres, Mon compte, Quitter #}
            <div class="mt-auto pt-4 border-top border-white border-opacity-10 d-flex flex-column gap-2 px-3 pb-3">
                <a href="{{ path('settings_index') }}" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none sidebar-link">
                    <i class="bi bi-gear fs-5"></i>
                    <span>{{ 'nav.footer.settings'|trans({}, 'navigation') }}</span>
                </a>
                <a href="{{ path('profile_index') }}" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none sidebar-link">
                    <i class="bi bi-person-circle fs-5"></i>
                    <span>{{ 'nav.footer.profile'|trans({}, 'navigation') }}</span>
                </a>
                <a href="{{ path('app_logout') }}" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none sidebar-link">
                    <i class="bi bi-box-arrow-right fs-5"></i>
                    <span>{{ 'nav.footer.logout'|trans({}, 'navigation') }}</span>
                </a>
            </div>
        </aside>

        {# Sidebar mobile - Offcanvas #}
        <aside class="offcanvas offcanvas-start d-lg-none" tabindex="-1" id="sidebarOffcanvas" aria-labelledby="sidebarOffcanvasLabel" style="--bs-offcanvas-width: 280px; --bs-offcanvas-bg: var(--theme-sidebar-bg); --bs-offcanvas-color: var(--theme-sidebar-text);">
            <div class="offcanvas-body d-flex flex-column p-0">
                {# Header avec icônes #}
                <div class="d-flex align-items-center justify-content-between mb-4 pb-3 px-3 pt-4">
                    <button class="btn btn-link text-white-50 p-0" type="button" style="font-size: 2rem;">
                        <i class="bi bi-list"></i>
                    </button>
                    <button class="btn btn-link text-white-50 p-0" type="button" data-bs-dismiss="offcanvas" aria-label="Fermer" style="font-size: 2rem;">
                        <i class="bi bi-arrow-left-circle"></i>
                    </button>
                </div>

                {# Navigation #}
                <nav class="flex-grow-1 overflow-auto px-3">
                    {# Section Favoris #}
                    <div class="nav-section">
                        <div class="nav-section-title-pill" data-bs-toggle="collapse" data-bs-target="#favorisSectionMobile" aria-expanded="true">
                            <div class="d-flex align-items-center gap-2">
                                <i class="bi bi-star"></i>
                                <span>{{ 'nav.section.favorites'|trans({}, 'navigation') }}</span>
                            </div>
                            <i class="bi bi-chevron-down"></i>
                        </div>
                        <div class="collapse show" id="favorisSectionMobile">
                            <ul class="list-unstyled">
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                        <span>{{ 'nav.favorites.invoices'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                        <span>{{ 'nav.favorites.orders'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                        <span>{{ 'nav.favorites.stocks'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none nav-link">
                                        <span>{{ 'nav.favorites.help'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>

                    {# Section Historique #}
                    <div class="nav-section">
                        <div class="nav-section-title-pill" data-bs-toggle="collapse" data-bs-target="#historiqueSectionMobile" aria-expanded="false">
                            <div class="d-flex align-items-center gap-2">
                                <i class="bi bi-clock-history"></i>
                                <span>{{ 'nav.section.history'|trans({}, 'navigation') }}</span>
                            </div>
                            <i class="bi bi-chevron-down"></i>
                        </div>
                        <div class="collapse" id="historiqueSectionMobile">
                            <ul class="list-unstyled">
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                        <span>{{ 'nav.history.last_conversation'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                        <span>{{ 'nav.history.invoices_jan'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                        <span>{{ 'nav.history.stock_analysis'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                        <span>{{ 'nav.history.orders_report'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="d-flex align-items-center px-3 py-2 rounded text-white-50 text-decoration-none small nav-link">
                                        <span>{{ 'nav.history.data_export'|trans({}, 'navigation') }}</span>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>

                {# Footer #}
                <div class="mt-auto pt-4 border-top border-white border-opacity-10 d-flex flex-column gap-2 px-3 pb-3">
                    <a href="{{ path('settings_index') }}" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none sidebar-link">
                        <i class="bi bi-gear fs-5"></i>
                        <span>{{ 'nav.footer.settings'|trans({}, 'navigation') }}</span>
                    </a>
                    <a href="{{ path('profile_index') }}" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none sidebar-link">
                        <i class="bi bi-person-circle fs-5"></i>
                        <span>{{ 'nav.footer.profile'|trans({}, 'navigation') }}</span>
                    </a>
                    <a href="{{ path('app_logout') }}" class="d-flex align-items-center gap-3 px-3 py-2 rounded text-white text-decoration-none sidebar-link">
                        <i class="bi bi-box-arrow-right fs-5"></i>
                        <span>{{ 'nav.footer.logout'|trans({}, 'navigation') }}</span>
                    </a>
                </div>
            </div>
        </aside>

        {# Zone principale - Margin left sur desktop pour sidebar permanente #}
        <main class="flex-grow-1 d-flex flex-column home-main">
            {# CONTAINER 1 : Header + Navigation sticky en haut #}
            <div class="sticky-top-container">
                {# Header avec logo à gauche et actions à droite (Bootstrap) #}
                <header class="home-header">
                    <div class="d-flex align-items-center justify-content-between px-4 py-3">
                        {# Logo à gauche : picto + texte - cliquable pour retourner au dashboard #}
                        <a href="{{ path('home_index') }}" class="d-flex align-items-center gap-2 text-decoration-none">
                            {# Picto adaptatif selon le thème #}
                            {% set theme = app.user ? app.user.theme : 'light' %}
                            {% if theme == 'light' %}
                                <img src="{{ asset('images/logo_picto.svg') }}" alt="CFI" class="header-logo-picto">
                            {% else %}
                                <img src="{{ asset('images/assistant-picto.svg') }}" alt="CFI" class="header-logo-picto">
                            {% endif %}
                            <img src="{{ asset('images/logo.svg') }}" alt="myCFiia" class="header-logo">
                        </a>

                        {# Actions à droite #}
                        <div class="d-flex align-items-center gap-3">
                            {{ component('ThemeSelector') }}
                            <a href="#" class="btn btn-link text-white text-decoration-none">
                                <i class="bi bi-box-arrow-up-right"></i>
                                {{ 'nav.header.back_to_cfi'|trans({}, 'navigation') }}
                            </a>
                        </div>
                    </div>
                </header>

                {# Navigation contextuelle (chat uniquement) #}
                {% block navigation %}{% endblock %}
            </div>

            {# CONTAINER 2 : Contenu scrollable #}
            <div class="home-content{% block content_classes %}{% endblock %}">
                {% block content %}{% endblock %}
            </div>

            {# CONTAINER 3 : Input sticky en bas (seulement pour les pages chat) #}
            {% block sticky_bottom %}{% endblock %}
        </main>
    </div>

    {% block javascripts %}
        {{ importmap('app') }}
        <script type="module" src="{{ asset('sidebar.js') }}"></script>
    {% endblock %}
</body>
</html>
