{% extends 'layouts/home.html.twig' %}

{% block title %}Génération Assets en cours - {{ project.name }}{% endblock %}

{% block content %}
<div class="container" style="max-width: 1000px;">
    <div class="mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="{{ path('marketing_project_index') }}">Projets Marketing</a>
                </li>
                <li class="breadcrumb-item">
                    <a href="{{ path('marketing_project_show', {id: project.id}) }}">{{ project.name }}</a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">Génération assets</li>
            </ol>
        </nav>
    </div>

    <div class="card shadow-sm"
         data-controller="marketing-generation"
         data-marketing-generation-task-id-value="{{ taskId }}"
         data-marketing-generation-project-id-value="{{ project.id }}"
         data-marketing-generation-stage-value="assets"
         data-marketing-generation-mercure-url-value="{{ mercureUrl }}"
         data-marketing-generation-mercure-jwt-value="{{ mercureJwt }}"
         data-marketing-generation-next-url-value="{{ path('marketing_project_show', {id: project.id}) }}"
         data-marketing-generation-multiple-value="true">

        <div class="card-header bg-primary text-white">
            <div class="d-flex align-items-center justify-content-between">
                <div class="d-flex align-items-center">
                    <div class="spinner-border spinner-border-sm me-3" role="status" data-marketing-generation-target="spinner">
                        <span class="visually-hidden">Chargement...</span>
                    </div>
                    <div>
                        <h5 class="mb-0">Génération Assets en cours...</h5>
                        <small>ContentCreatorAgent travaille sur vos contenus</small>
                    </div>
                </div>
                <div class="text-end">
                    <div class="small opacity-75">Progression globale</div>
                    <div class="fs-5 fw-bold" data-marketing-generation-target="overallProgress">0%</div>
                </div>
            </div>
        </div>

        <div class="card-body p-4">
            <div data-marketing-generation-target="statusMessage">
                <div class="text-center mb-4">
                    <i class="bi bi-file-earmark-text text-primary mb-3" style="font-size: 3rem;"></i>
                    <h4 class="mb-2">Création de vos contenus marketing</h4>
                    <p class="text-muted">
                        L'IA génère vos assets en parallèle pour maximiser la vitesse
                    </p>
                </div>

                {# Barre de progression temps réel (v3.34.0) #}
                <div class="mb-4">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <div class="fw-semibold text-primary" data-progress-message>
                            Initialisation...
                        </div>
                        <div class="badge bg-primary" data-phase-indicator>
                            Phase 0/3
                        </div>
                    </div>

                    <div class="progress mb-2" style="height: 30px;" role="progressbar" aria-label="Progression génération assets" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100">
                        <div class="progress-bar progress-bar-striped progress-bar-animated bg-primary"
                             style="width: 0%"
                             data-marketing-generation-target="progressBar"
                             data-progress-bar>
                            <span class="fw-semibold" data-marketing-generation-target="progressText" data-progress-percentage>0%</span>
                        </div>
                    </div>

                    <div class="text-muted small text-center">
                        <i class="bi bi-robot me-1"></i> ContentCreatorAgent génère vos assets en temps réel
                    </div>
                </div>

                <div class="mb-4" data-marketing-generation-target="assetsList">
                    <h6 class="mb-3">Assets en cours de génération :</h6>
                    <div class="row g-2" data-marketing-generation-target="assetsContainer">
                        <div class="col-12 text-center text-muted">
                            <div class="spinner-border spinner-border-sm me-2" role="status"></div>
                            Initialisation des tâches...
                        </div>
                    </div>
                </div>
            </div>

            <div class="d-none" data-marketing-generation-target="successMessage">
                <div class="alert alert-success text-center">
                    <i class="bi bi-check-circle-fill fs-1 text-success d-block mb-3"></i>
                    <h4 class="mb-3">Tous les assets ont été générés avec succès !</h4>
                    <p class="mb-3" data-marketing-generation-target="resultSummary"></p>
                    <div class="d-flex gap-2 justify-content-center">
                        <a href="{{ path('marketing_project_show', {id: project.id}) }}" class="btn btn-success">
                            <i class="bi bi-eye"></i> Voir tous les assets
                        </a>
                    </div>
                </div>
            </div>

            <div class="d-none" data-marketing-generation-target="errorMessage">
                <div class="alert alert-danger text-center">
                    <i class="bi bi-exclamation-triangle-fill fs-1 text-danger d-block mb-3"></i>
                    <h4 class="mb-3">Erreur lors de la génération</h4>
                    <p class="mb-3" data-marketing-generation-target="errorDetails"></p>
                    <div class="d-flex gap-2 justify-content-center">
                        <a href="{{ path('marketing_asset_new', {id: project.id}) }}" class="btn btn-danger">
                            <i class="bi bi-arrow-counterclockwise"></i> Réessayer
                        </a>
                        <a href="{{ path('marketing_project_show', {id: project.id}) }}" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Retour au projet
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="card-footer bg-light">
            <div class="row text-center small">
                <div class="col-3">
                    <div class="text-muted">Agent IA</div>
                    <div class="fw-semibold">Mistral Large</div>
                </div>
                <div class="col-3">
                    <div class="text-muted">Temps écoulé</div>
                    <div class="fw-semibold" data-marketing-generation-target="elapsedTime">0s</div>
                </div>
                <div class="col-3">
                    <div class="text-muted">Assets terminés</div>
                    <div class="fw-semibold" data-marketing-generation-target="completedCount">0</div>
                </div>
                <div class="col-3">
                    <div class="text-muted">Statut</div>
                    <div class="fw-semibold" data-marketing-generation-target="status">En cours</div>
                </div>
            </div>
        </div>
    </div>

    <div class="alert alert-info mt-4">
        <i class="bi bi-lightning-charge me-2"></i>
        <strong>Génération parallèle optimisée</strong><br>
        <small class="text-muted">
            Grâce à Symfony Messenger, tous les assets sont générés simultanément.
            Vous êtes notifié en temps réel via Mercure à chaque asset complété.
        </small>
    </div>
</div>
{% endblock %}
