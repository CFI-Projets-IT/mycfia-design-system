{% extends 'layouts/home.html.twig' %}

{% block title %}Génération en cours - {{ project.name }}{% endblock %}

{% block content %}
<div class="container" style="max-width: 900px;">
    <div class="mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="{{ path('marketing_project_index') }}">Projets Marketing</a>
                </li>
                <li class="breadcrumb-item">
                    <a href="{{ path('marketing_project_show', {id: project.id}) }}">{{ project.name }}</a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">Génération en cours</li>
            </ol>
        </nav>
    </div>

    <div class="card shadow-sm"
         data-controller="marketing-generation"
         data-marketing-generation-project-id-value="{{ project.id }}"
         data-marketing-generation-task-id-value="{{ taskId }}"
         data-marketing-generation-stage-value="personas"
         data-marketing-generation-mercure-url-value="{{ mercureUrl }}"
         data-marketing-generation-next-url-value="{{ path('marketing_persona_show', {id: project.id}) }}">

        <div class="card-header bg-primary text-white">
            <div class="d-flex align-items-center">
                <div class="spinner-border spinner-border-sm me-3" role="status" data-marketing-generation-target="spinner">
                    <span class="visually-hidden">Chargement...</span>
                </div>
                <div>
                    <h5 class="mb-0">Génération de Personas IA en cours...</h5>
                    <small>Notre agent IA analyse votre secteur</small>
                </div>
            </div>
        </div>

        <div class="card-body p-4">
            <div data-marketing-generation-target="statusMessage">
                <div class="text-center py-5">
                    <i class="bi bi-robot text-primary mb-3" style="font-size: 4rem;"></i>
                    <h4 class="mb-3">L'IA travaille pour vous</h4>
                    <p class="text-muted mb-4">
                        Mistral Large analyse votre secteur et génère des personas détaillés<br>
                        Temps estimé : 30 à 60 secondes par persona
                    </p>

                    <div class="progress mb-3" style="height: 25px;">
                        <div class="progress-bar progress-bar-striped progress-bar-animated bg-primary"
                             role="progressbar"
                             style="width: 0%"
                             data-marketing-generation-target="progressBar">
                            <span data-marketing-generation-target="progressText">0%</span>
                        </div>
                    </div>

                    <div class="text-muted small" data-marketing-generation-target="progressMessage">
                        Initialisation...
                    </div>
                </div>
            </div>

            <div class="d-none" data-marketing-generation-target="successMessage">
                <div class="alert alert-success text-center">
                    <i class="bi bi-check-circle-fill fs-1 text-success d-block mb-3"></i>
                    <h4 class="mb-3">Personas générés avec succès !</h4>
                    <p class="mb-3" data-marketing-generation-target="resultSummary"></p>
                    <a href="{{ path('marketing_project_show', {id: project.id}) }}" class="btn btn-success">
                        <i class="bi bi-arrow-right-circle"></i> Voir les personas
                    </a>
                </div>
            </div>

            <div class="d-none" data-marketing-generation-target="errorMessage">
                <div class="alert alert-danger text-center">
                    <i class="bi bi-exclamation-triangle-fill fs-1 text-danger d-block mb-3"></i>
                    <h4 class="mb-3">Erreur lors de la génération</h4>
                    <p class="mb-3" data-marketing-generation-target="errorDetails"></p>
                    <div class="d-flex gap-2 justify-content-center">
                        <a href="{{ path('marketing_persona_generate', {id: project.id}) }}" class="btn btn-danger">
                            <i class="bi bi-arrow-counterclockwise"></i> Réessayer
                        </a>
                        <a href="{{ path('marketing_project_show', {id: project.id}) }}" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Retour au projet
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="card-footer bg-light">
            <div class="row text-center">
                <div class="col-4">
                    <div class="text-muted small">Agent IA</div>
                    <div class="fw-semibold">Mistral Large</div>
                </div>
                <div class="col-4">
                    <div class="text-muted small">Temps écoulé</div>
                    <div class="fw-semibold" data-marketing-generation-target="elapsedTime">0s</div>
                </div>
                <div class="col-4">
                    <div class="text-muted small">Statut</div>
                    <div class="fw-semibold" data-marketing-generation-target="status">En cours</div>
                </div>
            </div>
        </div>
    </div>

    <div class="alert alert-info mt-4">
        <i class="bi bi-info-circle me-2"></i>
        <strong>Cette page se met à jour automatiquement</strong><br>
        <small class="text-muted">
            Grâce à Mercure (Server-Sent Events), vous êtes notifié en temps réel de la progression.
            Pas besoin de rafraîchir la page manuellement.
        </small>
    </div>
</div>
{% endblock %}
