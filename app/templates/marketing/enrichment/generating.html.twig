{% extends 'layouts/home.html.twig' %}

{% block title %}Enrichissement en cours - {{ project.name }}{% endblock %}

{% block content %}
<div class="container" style="max-width: 900px;">
    <div class="mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="{{ path('marketing_project_index') }}">Projets Marketing</a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">Enrichissement en cours</li>
            </ol>
        </nav>
    </div>

    <div class="card shadow-sm"
         data-controller="marketing-enrichment"
         data-marketing-enrichment-project-id-value="{{ project.id }}"
         data-marketing-enrichment-task-id-value="{{ taskId }}"
         data-marketing-enrichment-mercure-url-value="{{ mercureUrl }}"
         data-marketing-enrichment-mercure-jwt-value="{{ mercureJwt }}"
         data-marketing-enrichment-next-url-value="{{ path('marketing_project_enrichment_review', {taskId: taskId}) }}">

        <div class="card-header bg-primary text-white">
            <div class="d-flex align-items-center">
                <div class="spinner-border spinner-border-sm me-3" role="status" data-marketing-enrichment-target="spinner">
                    <span class="visually-hidden">Chargement...</span>
                </div>
                <div>
                    <h5 class="mb-0">Enrichissement IA en cours...</h5>
                    <small>Notre agent IA analyse votre projet</small>
                </div>
            </div>
        </div>

        <div class="card-body p-4">
            <div data-marketing-enrichment-target="statusMessage">
                <div class="text-center py-5">
                    <i class="bi bi-robot text-primary mb-3" style="font-size: 4rem;"></i>
                    <h4 class="mb-3">L'IA travaille pour vous</h4>
                    <p class="text-muted mb-4">
                        Mistral Large analyse votre projet et génère des suggestions enrichies<br>
                        Temps estimé : 30 à 60 secondes
                    </p>

                    {# Barre de progression temps réel (v3.34.0) #}
                    <div class="mb-4">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <div class="fw-semibold text-primary" data-marketing-enrichment-target="progressMessage">
                                Initialisation...
                            </div>
                            <div class="badge bg-primary" data-marketing-enrichment-target="phaseIndicator">
                                Phase 0/9
                            </div>
                        </div>

                        <div class="progress mb-2" style="height: 25px;" role="progressbar" aria-label="Progression enrichissement" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100">
                            <div class="progress-bar progress-bar-striped progress-bar-animated bg-primary"
                                 style="width: 0%"
                                 data-marketing-enrichment-target="progressBar">
                                <span class="fw-semibold" data-marketing-enrichment-target="progressPercentage">0%</span>
                            </div>
                        </div>

                        <div class="text-muted small text-center">
                            <i class="bi bi-robot me-1"></i> Mistral Large analyse votre projet en temps réel
                        </div>
                    </div>

                    <div class="text-muted small text-center">
                        <i class="bi bi-clock"></i>
                        Temps écoulé : <span data-marketing-enrichment-target="elapsedTime">0s</span>
                    </div>
                </div>
            </div>

            <div class="d-none" data-marketing-enrichment-target="successMessage">
                <div class="alert alert-success text-center">
                    <i class="bi bi-check-circle-fill fs-1 text-success d-block mb-3"></i>
                    <h4 class="mb-3">Enrichissement terminé avec succès !</h4>
                    <p class="mb-3" data-marketing-enrichment-target="resultSummary"></p>
                    <div class="spinner-border text-success mb-3" role="status">
                        <span class="visually-hidden">Redirection...</span>
                    </div>
                    <p class="text-muted small">Redirection vers la page de révision...</p>
                </div>
            </div>

            <div class="d-none" data-marketing-enrichment-target="errorMessage">
                <div class="alert alert-danger text-center">
                    <i class="bi bi-exclamation-triangle-fill fs-1 text-danger d-block mb-3"></i>
                    <h4 class="mb-3">Erreur lors de l'enrichissement</h4>
                    <p class="mb-3" data-marketing-enrichment-target="errorDetails"></p>
                    <div class="d-flex gap-2 justify-content-center">
                        <a href="{{ path('home_index') }}" class="btn btn-danger">
                            <i class="bi bi-house"></i> Retour à l'accueil
                        </a>
                        <a href="{{ path('marketing_project_new') }}" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Créer un nouveau projet
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="card-footer bg-light">
            <div class="row text-center">
                <div class="col-4">
                    <div class="text-muted small">Agent IA</div>
                    <div class="fw-semibold">Mistral Large</div>
                </div>
                <div class="col-4">
                    <div class="text-muted small">Temps écoulé</div>
                    <div class="fw-semibold" data-marketing-enrichment-target="elapsedTime">0s</div>
                </div>
                <div class="col-4">
                    <div class="text-muted small">Statut</div>
                    <div class="fw-semibold" data-marketing-enrichment-target="status">En cours</div>
                </div>
            </div>
        </div>
    </div>

    <div class="alert alert-info mt-4">
        <i class="bi bi-info-circle me-2"></i>
        <strong>Cette page se met à jour automatiquement</strong><br>
        <small class="text-muted">
            Grâce à Mercure (Server-Sent Events), vous êtes notifié en temps réel de la progression.
            Pas besoin de rafraîchir la page manuellement.
        </small>
    </div>
</div>
{% endblock %}
